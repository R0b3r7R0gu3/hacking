# requirements.txt

scapy           # Per la manipolazione dei pacchetti di rete
requests        # Per le richieste HTTP, usato nei moduli di phishing e altre attività
beautifulsoup4  # Per l'analisi del contenuto HTML (per il phishing)
colorama        # Per colorare i messaggi di output nel terminale (opzionale)
cryptography    # Per il cracking delle password e per la crittografia
pyautogui       # Per interazioni con l'interfaccia grafica (es. per attacchi automatizzati)
pynput          # Per l'input dell'utente (es. per il phishing)
pyperclip       # Per copiare e incollare negli appunti (es. per il phishing)
pyfiglet        # Per generare testo ASCII art (opzionale, per l'estetica)
pyinstaller     # Per creare eseguibili standalone (opzionale, per la distribuzione)
flask          # Per creare un server web (opzionale, per il phishing e altre attività)